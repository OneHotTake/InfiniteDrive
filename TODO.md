# InfiniteDrive — TODO

## Open Items

- [ ] **RSS user limits** — Define per-user RSS feed limits (max feeds, max items per feed). Consider: should admin-configurable system feeds count toward a user's limit? Should there be a global cap and a per-user cap?
